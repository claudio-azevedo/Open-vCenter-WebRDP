name: Build and push webrdp image

on:
  push:
    branches: [main]
    paths:
      - "webrdp/**"
      - ".github/workflows/build-push-webrdp.yml"
    tags:
      - "*.*.*"
  workflow_dispatch: {}

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/ovc-webrdp

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        id: tags
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            TAGS="${{ env.IMAGE }}:${{ github.ref_name }}"
          else
            TAGS="${{ env.IMAGE }}:latest
          ${{ env.IMAGE }}:${{ github.sha }}"
          fi
          {
            echo "list<<EOF"
            echo "$TAGS"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: webrdp
          platforms: linux/amd64
          push: true
          tags: ${{ steps.tags.outputs.list }}
